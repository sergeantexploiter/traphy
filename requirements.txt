# Smart Traffic Light: vehicle detection, tracking, counting, speed, violations
# Python 3.8+

# YOLOv8 (CPU/GPU .pt/.onnx). Not required on the Orange Pi if you only load .rknn.
ultralytics>=8.0.0

# Orange Pi RK3588 NPU (6 TOPS) — install these ON THE BOARD, not on macOS:
#   pip3 install rknn-toolkit-lite2          # inference (.rknn / .rnn)
#   pip3 install rknn-toolkit2               # conversion (or the official wheel
#                                            # from https://github.com/airockchip/rknn-toolkit2)
# Convert:  python3 src/rknn_export.py --model yolov8n.pt --target rk3588 --dtype fp

# Video & image
opencv-python>=4.8.0
numpy>=1.24.0

# SORT tracker (Kalman filter + assignment)
filterpy>=1.4.5
scipy>=1.10.0

# MQTT (src/lane_detector.py edge publisher, coordinator, simulator)
paho-mqtt>=1.6.0

# License plate recognition / ANPR (src/plate_reader.py, vehicle/main.py)
# EasyOCR is the default OCR engine (pulls in torch, already needed by ultralytics).
easyocr>=1.7.0
# Optional alternative OCR engine for plate_reader.py --ocr tesseract
# (also needs the system 'tesseract' binary: brew install tesseract / apt install tesseract-ocr)
# pytesseract>=0.3.10
